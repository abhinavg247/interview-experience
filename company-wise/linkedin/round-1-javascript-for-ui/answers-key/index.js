class Calculator {
  constructor() {
    this.inputArr = [];
  }

  init() {
    // use event delegation when attaching event listeners
    // event listener only needs to be attached to the container of buttons
    // use button type and innerHTML to handle numbers vs operators
  }
}
